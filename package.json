{
  "name": "research-intelligence-system",
  "version": "0.4.2",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --configLoader native",
    "build": "vite build --configLoader native",
    "preview": "vite preview --host 0.0.0.0 --configLoader native",
    "start": "npm run build && node scripts/start.js",
    "start:api": "node server.js",
    "worker": "python -m worker.service",
    "check": "node --check server.js && node --check scripts/start.js && node --check server/env.js && node --check server/db.js && node --check server/events.js && node --check server/settings.js && node --check server/jobs.js && node --check server/health.js && node --check server/notifications.js && node --check server/outbox.js && node --check server/artifacts.js && node --check server/library.js && node --check server/projects.js && node --check server/reader.js && node --check server/papers.js && node --check server/workerQueue.js && node --check server/uploadStaging.js && node --check src/lib/cacheKeys.js && node --check src/lib/markdownMath.js && node --check src/lib/serverEventRules.js && npm run test:node && npm run build && python -m compileall worker tests",
    "init-db": "python -m worker.cli init-db",
    "sync-obsidian": "python -m worker.cli sync-obsidian",
    "fetch-arxiv": "python -m worker.cli fetch-arxiv",
    "cache-arxiv-text": "python -m worker.cli cache-arxiv-text",
    "generate-paper-reports": "python -m worker.cli generate-paper-reports",
    "generate-reports": "python -m worker.cli generate-reports",
    "run-daily": "python -m worker.cli run-daily",
    "test": "npm run test:python && npm run test:node",
    "test:python": "python -m unittest discover -s tests",
    "test:node": "node --test tests/node/*.test.js"
  },
  "engines": {
    "node": ">=22.12.0",
    "python": ">=3.11"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "katex": "^0.16.45",
    "pg": "^8.22.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "react-markdown": "^10.1.0",
    "react-router-dom": "^7.15.1",
    "rehype-katex": "^7.0.1",
    "remark-gfm": "^4.0.1",
    "remark-math": "^6.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "vite": "^8.0.10"
  }
}
